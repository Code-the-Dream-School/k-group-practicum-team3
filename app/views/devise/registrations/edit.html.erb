<div class="max-w-3xl mx-auto bg-white text-gray-900 border border-gray-300 rounded-md shadow-sm">
  <!-- Header -->
  <div class="px-6 py-4 border-b border-gray-300">
    <h3 class="text-xl font-semibold">Edit Profile</h3>
  </div>

  <div class="px-6 py-6 space-y-6">

    <%# Form for updating user %>
    <%= form_for(resource, as: resource_name,
                 url: registration_path(resource_name),
                 html: { method: :put, multipart: true }) do |f| %>

      <%# Styled validation errors %>
      <% if resource.errors.any? %>
        <div class="mb-4 p-3 border border-red-400 bg-red-50 text-red-700 rounded">
          <h4 class="font-semibold mb-2">Please fix the following errors:</h4>
          <ul class="list-disc list-inside text-sm">
            <% resource.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <!-- Profile Picture -->
      <div>
        <%= f.label :profile_picture, class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= f.file_field :profile_picture, accept: "image/*", class: "block w-full text-sm" %>

        <% if resource.profile_picture.attached? %>
          <div class="mt-3">
            <%= image_tag resource.profile_picture,
                class: "rounded-full border border-gray-300 w-32 h-32 object-cover" %>
          </div>
        <% end %>
      </div>

      <!-- FIRST / LAST -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div>
          <%= f.label :first_name, class: "block text-sm font-medium text-gray-700 mb-1" %>
          <%= f.text_field :first_name,
              class: "w-full border border-gray-300 rounded-md px-3 py-2 text-sm" %>
        </div>

        <div>
          <%= f.label :last_name, class: "block text-sm font-medium text-gray-700 mb-1" %>
          <%= f.text_field :last_name,
              class: "w-full border border-gray-300 rounded-md px-3 py-2 text-sm" %>
        </div>
      </div>

      <!-- AGE / GENDER -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div>
          <%= f.label :age, class: "block text-sm font-medium text-gray-700 mb-1" %>
          <%= f.number_field :age,
              class: "w-full border border-gray-300 rounded-md px-3 py-2 text-sm" %>
        </div>

        <div>
          <%= f.label :gender, class: "block text-sm font-medium text-gray-700 mb-1" %>
          <%= f.select :gender,
              User.genders.keys.map { |g| [g.humanize, g] },
              { include_blank: true },
              class: "w-full border border-gray-300 rounded-md px-3 py-2 text-sm" %>
        </div>
      </div>

      <!-- LOCATION -->
      <div>
        <%= f.label :city, "Location (City)", class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= f.text_field :city,
            class: "w-full border border-gray-300 rounded-md px-3 py-2 text-sm" %>
      </div>

      <div>
        <%= f.label :state, "Location (State)", class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= f.text_field :state,
            class: "w-full border border-gray-300 rounded-md px-3 py-2 text-sm" %>
      </div>

      <div>
        <%= f.label :zip, "Location (ZIP Code)", class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= f.text_field :zip,
            class: "w-full border border-gray-300 rounded-md px-3 py-2 text-sm" %>
      </div>

      <!-- BIO -->
      <div>
        <%= f.label :bio, class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= f.text_area :bio,
            rows: 3,
            class: "w-full border border-gray-300 rounded-md px-3 py-2 text-sm" %>
      </div>

      <!-- SECURITY -->
      <div class="pt-4">
        <p class="text-sm text-gray-500 mb-3">
          Leave blank if you don&#39;t want to change your password.
        </p>

        <div class="space-y-3">
          <div>
            <%= f.label :password, class: "block text-sm font-medium text-gray-700 mb-1" %>
            <%= f.password_field :password,
                autocomplete: "new-password",
                class: "w-full border border-gray-300 rounded-md px-3 py-2 text-sm" %>
          </div>

          <div>
            <%= f.label :password_confirmation, class: "block text-sm font-medium text-gray-700 mb-1" %>
            <%= f.password_field :password_confirmation,
                autocomplete: "new-password",
                class: "w-full border border-gray-300 rounded-md px-3 py-2 text-sm" %>
          </div>

          <div>
            <%= f.label :current_password,
                "Current password (required to save changes)",
                class: "block text-sm font-medium text-gray-700 mb-1" %>

            <%= f.password_field :current_password,
                autocomplete: "current-password",
                class: "w-full border border-gray-300 rounded-md px-3 py-2 text-sm" %>
          </div>
        </div>
      </div>

      <!-- Form Actions -->
      <div class="mt-6 flex space-x-4">
        <%= link_to "Cancel", user_path(current_user),
          class: "px-4 py-2 bg-gray-600 text-white rounded-md hover:bg-gray-700 text-sm" %>

        <%= f.submit "Update",
          class: "px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 text-sm" %>
      </div>

    <% end %>
  </div>
</div>
